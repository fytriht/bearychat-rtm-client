const RTMClientState = {
  INITIAL: 'initial',
  CONNECTING: 'connecting',
  CONNECTED: 'connected',
  RECONNECT: 'reconnect',
  CLOSING: 'closing',
  CLOSED: 'closed'
};

export default RTMClientState;
